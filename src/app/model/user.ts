export interface User {
    username?: string,
    password?: string,
    email?: string,
    roles?: any,
    status?: number
    ava?: string,
    name?: string,
    address?: string,
    phone?: string,
}
